{
  "name": "rentals-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "postinstall": "npm run -w @rentals/db generate",
    "db:generate": "npm run -w @rentals/db generate",
    "db:push": "npm run -w @rentals/db push",
    "db:seed": "npm run -w @rentals/db seed",
    "db:reset": "npm run -w @rentals/db reset",
    "env": "node scripts/setup-env.mjs",
    "setup": "npm install && npm run env && npm run db:generate && npm run db:push && npm run db:seed",
    "dev:web": "npm run -w @rentals/web dev",
    "dev:admin": "npm run -w @rentals/admin dev",
    "dev": "concurrently -n web,admin -c blue,magenta \"npm run dev:web\" \"npm run dev:admin\"",
    "build": "npm run -w @rentals/web build && npm run -w @rentals/admin build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "next": "^14.2.35"
  }
}
